from datetime import datetime
import logging
import os
from housekeeping import _make_timestamp

def configure_logging(name=None, local=False, path=os.getcwd()):
    if not local:
        pass
    else:
        pass

def make_log_file():
    pass
